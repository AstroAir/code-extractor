"""
CLI entry point for pysearch.

This module serves as the entry point when pysearch.cli is executed as a module
with `python -m pysearch.cli`.
"""

from .main import cli

if __name__ == "__main__":
    cli()
