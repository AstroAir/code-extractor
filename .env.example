# 环境变量示例（请复制为 .env 并按需调整）
# Python 运行环境
PYTHONWARNINGS=default

# 日志级别（如后续引入 logging）
PYSEARCH_LOG_LEVEL=INFO

# 示例：额外的 include / exclude（CLI 参数优先生效）
PYSEARCH_INCLUDE=**/*.py
PYSEARCH_EXCLUDE=**/.venv/**,**/.git/**,**/build/**,**/dist/**,**/__pycache__/**